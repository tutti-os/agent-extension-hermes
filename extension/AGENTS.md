# Hermes Agent package

The runtime provider is `acp:hermes` and the signing key ID is `tutti-hermes-release-v1`. Keep all package content declarative.

`icon.png` is the shared branded identity used in the Provider Rail,
conversation rows, conversation headers, Message Center, and mentions. Hermes
does not declare a mask icon, so consumers fall back to this primary icon.
