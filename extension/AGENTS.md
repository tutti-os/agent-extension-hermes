# Hermes Agent package

The runtime provider is `acp:hermes` and the signing key ID is `tutti-hermes-release-v1`. Keep all package content declarative.

`icon.svg` is the colored shared branded identity used in the Provider Rail,
conversation headers, Message Center, and mentions. `mask-icon.svg` is the
transparent conversation-row mask glyph.
