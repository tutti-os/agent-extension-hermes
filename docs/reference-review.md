# Reference review

The runtime package, `acp` extra, CLI entry point, and ACP subcommand are owned
by the upstream `NousResearch/hermes-agent` project. Tutti's package carries no
upstream executable and installs the exact PyPI release only after confirmation.
